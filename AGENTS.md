# AGENTS.md — Working with Claude on This Repo

This repo is designed to be used with Claude Code as a research and documentation partner. Each topic is a structured markdown file. Claude researches, writes, and keeps files current.

---

## Slash commands

| Command | What it does |
|---------|-------------|
| `/research-topic <name>` | Research a topic end-to-end: web search → write doc → add Mermaid diagram → update README status → commit |

**Example usage:**
```
/research-topic speculative decoding
/research-topic A2A protocol
/research-topic DPO & GRPO
```

---

## Research workflow

1. Find the topic in README.md — it will have a 🔴 status and link to a placeholder file
2. Run `/research-topic <topic name>` in Claude Code
3. Claude will: search the web + papers, write the full doc, generate a diagram, update the status icon, and commit
4. Review the result — add your own observations to `## My notes`
5. If a demo is needed, build it in `demos/` and link it from `## Code / demo`

---

## Revisiting topics

AI moves fast. Completed topics (🟢) should be revisited periodically as the ecosystem evolves. To update:

```
/research-topic <topic name>
```

If the file already exists with content, Claude will update it with new findings rather than overwriting.

---

## Diagrams

**In `.md` files:** Diagrams are written as ` ```mermaid ` code blocks. They render natively on GitHub and in VS Code (install the [Mermaid Preview](https://marketplace.visualstudio.com/items?itemName=bierner.markdown-mermaid) extension).

**Reference images:** Static PNGs and SVGs live in `assets/images/`. Topic-specific diagrams are generated by the `image-finder` agent and stored in `assets/images/topics/<section>/`.

**Current reference images:**
- `assets/images/architecture.png` — full system architecture (repo-level reference)

---

## Topic template

See CLAUDE.md for the full section template every topic file follows.

---

## Section map

| Section | What it covers |
|---------|----------------|
| `topics/01-model-inference-core/` | Core LLM mechanics: tokenization, attention, KV cache, batching, decoding |
| `topics/02-prompting-control/` | Prompting, sampling, structured output, prompt versioning |
| `topics/03-serving-infrastructure/` | vLLM, TGI, TensorRT-LLM, parallelism strategies, serving metrics |
| `topics/04-model-optimization/` | Quantization, fine-tuning, LoRA, RLHF, DPO, distillation |
| `topics/05-retrieval-memory/` | RAG, vector DBs, chunking, hybrid search, GraphRAG, agent memory |
| `topics/06-agents-orchestration/` | Agents, MCP, LangGraph, multi-agent, tool calling, A2A |
| `topics/07-safety-governance/` | Guardrails, PII, red-teaming, EU AI Act, NIST RMF |
| `topics/08-evaluation-quality/` | RAGAS, LLM-as-judge, golden datasets, CI/CD eval gates |
| `topics/09-observability-ops/` | LLMOps, tracing, drift, cost tracking, Langfuse, audit logs |
| `topics/10-integration-cloud/` | Gateways, routing, streaming, cloud platforms, hybrid deployment |
